model:
  image_channels: 3
  imu_dim: 10
  hidden_dim: 64
  weights: ""  # Optional path to .pth/.pt weights; leave empty for random init

preprocess:
  image:
    normalize_mean: [0.485, 0.456, 0.406]
    normalize_std: [0.229, 0.224, 0.225]
  imu:
    scale: 1.0
